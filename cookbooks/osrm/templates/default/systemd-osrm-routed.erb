# File maintained by Chef. Do not edit.

[Unit]
Description=Open Source Routing Machine - Profile <%= @profile %>
After=syslog.target network.target

[Service]
Type=simple
User=osrm
Group=osrm
ExecStart=<%= @basedir %>/osrm-backend/build/osrm-routed --algorithm=MLD -i 127.0.0.1 -p <%= @port %> -t 6 <%= @basedir %>/data/<%= @profile %>.osrm

[Install]
WantedBy=multi-user.target
